#include <iostream>
#include <iomanip>

using namespace std;

int main() {
    int N;
    cin >> N;

    // Output the number in hexadecimal format (uppercase)
    cout << hex << uppercase << N << endl;
}
